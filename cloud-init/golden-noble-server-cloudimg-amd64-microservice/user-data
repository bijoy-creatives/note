#cloud-config
users:
  - name: ubuntu
    sudo: ALL=(ALL) NOPASSWD:ALL
    shell: /bin/bash
    plain_text_passwd: "ubuntu"    # password login
    lock_passwd: false
    ssh_authorized_keys:
      - ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIKUV/RYJ0QwXblR3YtHyJGG97/Qu+Mv/mU0KEBQUKsZf bijoy@creative  # your public key
ssh_pwauth: true                  # enable password SSH login
chpasswd:
  expire: False
#Ensure package index is updated and system upgraded
package_update: true
package_upgrade: true
package_reboot_if_required: true   # optional: reboot if kernel or core packages require it

# Install Maven directly via packages
runcmd:
  - sudo apt install -y openjdk-21-jre-headless # version 21.0.8+9~us1-0ubuntu1~24.04.1
  - sudo apt install -y maven